#include <stdio.h>
/***
 * main - Entry
 * Description: print single digit in base10
 * Return: Always (0)
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	printf("\n");
	return (0);
}
