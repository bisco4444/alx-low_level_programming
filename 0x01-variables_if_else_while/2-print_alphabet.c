#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Prints alphabets in lowercase'
 * Reaturn: Always 0 (success)
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch)
	}
			return (0);
}
